node_modules
.next
out
dist
build
.vercel
.git
*.log
.env*
package-lock.json
yarn.lock

# Sanity Studio is a separate npm project with its own Prettier config
# (printWidth 100, bracketSpacing false). Formatting it from the root config
# would fight that config on every save.
studio-effuse.io

# Generated Sanity runtime output
.sanity